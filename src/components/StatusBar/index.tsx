import React from 'react';
import { StatusBar } from 'react-native';

import colors from '_config/colors';

export default () => (<StatusBar backgroundColor={colors.stainedWhite} barStyle="dark-content" />)